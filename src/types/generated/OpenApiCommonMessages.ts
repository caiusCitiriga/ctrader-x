// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v3.19.1
// source: OpenApiCommonMessages.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { ProtoPayloadType, protoPayloadTypeFromJSON, protoPayloadTypeToJSON } from "./OpenApiCommonModelMessages";

export const protobufPackage = "";

/** Base message that is used for all messages that are sent to/from Open API proxy of cTrader platform. */
export interface ProtoMessage {
  /** Contains id of ProtoPayloadType or other custom PayloadTypes (e.g. ProtoOAPayloadType). */
  payloadType: number;
  /** Serialized protobuf message that corresponds to payloadType. */
  payload?:
    | Uint8Array
    | undefined;
  /** Request message id, assigned by the client that will be returned in the response. */
  clientMsgId?: string | undefined;
}

/** Error response that is sent from Open API proxy when error occurs. */
export interface ProtoErrorRes {
  payloadType?:
    | ProtoPayloadType
    | undefined;
  /** Contains name of ProtoErrorCode or other custom ErrorCodes (e.g. ProtoCHErrorCode). */
  errorCode: string;
  /** Error description. */
  description?:
    | string
    | undefined;
  /** The Unix time in milliseconds of the end of the maintenance. */
  maintenanceEndTimestamp?: number | undefined;
}

/** Event that is sent from Open API proxy and can be used as criteria that connection is healthy when no other messages are sent by cTrader platform. Open API client can send this message when he needs to keep the connection open for a period without other messages longer than 30 seconds. */
export interface ProtoHeartbeatEvent {
  payloadType?: ProtoPayloadType | undefined;
}

function createBaseProtoMessage(): ProtoMessage {
  return { payloadType: 0, payload: new Uint8Array(0), clientMsgId: "" };
}

export const ProtoMessage: MessageFns<ProtoMessage> = {
  encode(message: ProtoMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.payloadType !== 0) {
      writer.uint32(8).uint32(message.payloadType);
    }
    if (message.payload !== undefined && message.payload.length !== 0) {
      writer.uint32(18).bytes(message.payload);
    }
    if (message.clientMsgId !== undefined && message.clientMsgId !== "") {
      writer.uint32(26).string(message.clientMsgId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProtoMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProtoMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.payloadType = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.payload = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.clientMsgId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProtoMessage {
    return {
      payloadType: isSet(object.payloadType) ? globalThis.Number(object.payloadType) : 0,
      payload: isSet(object.payload) ? bytesFromBase64(object.payload) : new Uint8Array(0),
      clientMsgId: isSet(object.clientMsgId) ? globalThis.String(object.clientMsgId) : "",
    };
  },

  toJSON(message: ProtoMessage): unknown {
    const obj: any = {};
    if (message.payloadType !== 0) {
      obj.payloadType = Math.round(message.payloadType);
    }
    if (message.payload !== undefined && message.payload.length !== 0) {
      obj.payload = base64FromBytes(message.payload);
    }
    if (message.clientMsgId !== undefined && message.clientMsgId !== "") {
      obj.clientMsgId = message.clientMsgId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProtoMessage>, I>>(base?: I): ProtoMessage {
    return ProtoMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProtoMessage>, I>>(object: I): ProtoMessage {
    const message = createBaseProtoMessage();
    message.payloadType = object.payloadType ?? 0;
    message.payload = object.payload ?? new Uint8Array(0);
    message.clientMsgId = object.clientMsgId ?? "";
    return message;
  },
};

function createBaseProtoErrorRes(): ProtoErrorRes {
  return { payloadType: 50, errorCode: "", description: "", maintenanceEndTimestamp: 0 };
}

export const ProtoErrorRes: MessageFns<ProtoErrorRes> = {
  encode(message: ProtoErrorRes, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.payloadType !== undefined && message.payloadType !== 50) {
      writer.uint32(8).int32(message.payloadType);
    }
    if (message.errorCode !== "") {
      writer.uint32(18).string(message.errorCode);
    }
    if (message.description !== undefined && message.description !== "") {
      writer.uint32(26).string(message.description);
    }
    if (message.maintenanceEndTimestamp !== undefined && message.maintenanceEndTimestamp !== 0) {
      writer.uint32(32).uint64(message.maintenanceEndTimestamp);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProtoErrorRes {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProtoErrorRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.payloadType = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.errorCode = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.maintenanceEndTimestamp = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProtoErrorRes {
    return {
      payloadType: isSet(object.payloadType) ? protoPayloadTypeFromJSON(object.payloadType) : 50,
      errorCode: isSet(object.errorCode) ? globalThis.String(object.errorCode) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      maintenanceEndTimestamp: isSet(object.maintenanceEndTimestamp)
        ? globalThis.Number(object.maintenanceEndTimestamp)
        : 0,
    };
  },

  toJSON(message: ProtoErrorRes): unknown {
    const obj: any = {};
    if (message.payloadType !== undefined && message.payloadType !== 50) {
      obj.payloadType = protoPayloadTypeToJSON(message.payloadType);
    }
    if (message.errorCode !== "") {
      obj.errorCode = message.errorCode;
    }
    if (message.description !== undefined && message.description !== "") {
      obj.description = message.description;
    }
    if (message.maintenanceEndTimestamp !== undefined && message.maintenanceEndTimestamp !== 0) {
      obj.maintenanceEndTimestamp = Math.round(message.maintenanceEndTimestamp);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProtoErrorRes>, I>>(base?: I): ProtoErrorRes {
    return ProtoErrorRes.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProtoErrorRes>, I>>(object: I): ProtoErrorRes {
    const message = createBaseProtoErrorRes();
    message.payloadType = object.payloadType ?? 50;
    message.errorCode = object.errorCode ?? "";
    message.description = object.description ?? "";
    message.maintenanceEndTimestamp = object.maintenanceEndTimestamp ?? 0;
    return message;
  },
};

function createBaseProtoHeartbeatEvent(): ProtoHeartbeatEvent {
  return { payloadType: 51 };
}

export const ProtoHeartbeatEvent: MessageFns<ProtoHeartbeatEvent> = {
  encode(message: ProtoHeartbeatEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.payloadType !== undefined && message.payloadType !== 51) {
      writer.uint32(8).int32(message.payloadType);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProtoHeartbeatEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProtoHeartbeatEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.payloadType = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProtoHeartbeatEvent {
    return { payloadType: isSet(object.payloadType) ? protoPayloadTypeFromJSON(object.payloadType) : 51 };
  },

  toJSON(message: ProtoHeartbeatEvent): unknown {
    const obj: any = {};
    if (message.payloadType !== undefined && message.payloadType !== 51) {
      obj.payloadType = protoPayloadTypeToJSON(message.payloadType);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProtoHeartbeatEvent>, I>>(base?: I): ProtoHeartbeatEvent {
    return ProtoHeartbeatEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProtoHeartbeatEvent>, I>>(object: I): ProtoHeartbeatEvent {
    const message = createBaseProtoHeartbeatEvent();
    message.payloadType = object.payloadType ?? 51;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
